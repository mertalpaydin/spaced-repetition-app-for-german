name: CI Quality Gate

on:
  push:
    branches: [ main, "feat/*" ]
  pull_request:
    branches: [ main ]

jobs:
  quality-checks:
    name: Lint, Type Check & Test
    # The whole suite: no network, no key. This is the gate every push and PR
    # must pass.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: uv sync

      - name: Ruff Format Check
        run: uv run ruff format --check src tests scripts

      - name: Ruff Lint Check
        run: uv run ruff check src tests scripts

      - name: Mypy Strict Type Check
        run: uv run mypy --strict src/ scripts/

      - name: Validate the committed deck
        run: uv run python scripts/build_phrase_deck.py --check

      - name: Run Test Suite with Coverage Gate (>= 85%)
        run: uv run pytest --cov=src --cov-fail-under=85 tests/
